# 练习 B · 定位分块是否改变信息

> 模板 —— 复制这份文件填写，不要直接改这份原始模板。对应 `L3_LLM摘要与长文本.md` 的「作业 2」。

## 输入身份 / 模式

- 运行目录（single）：
- 运行目录（map-reduce，`--strategy map-reduce --chunk-tokens 2000`）：
- 数据来源（`source`）：
- 模型 / mock：

## Single 摘要

```text
(粘贴单次摘要输出)
```

## Map-Reduce 摘要

```text
(粘贴 Reduce 后的最终摘要输出；如需引用中间结果，从 summary_runs.jsonl 的 partials 里摘录)
```

## 事实链：原文 → Map → Reduce

> 选一个具体事实（数字 / 时间 / 状态 / 负责人），追踪它在三个阶段分别是怎么写的。

- 原文：
- Map 阶段（对应 partial）：
- Reduce 阶段（最终摘要）：
- 结论（遗漏 / 重复 / 状态错误 / 一致，任选其一并说明）：

## 改进建议

> 提出一个具体改进（例如：按线程分块、相邻 overlap、先提事实再汇总等）。

-

## 备注

> 允许结论「本次未观察到问题」，但必须给出对照证据；mock 的机械截取不等同于真实模型跨块理解失败，分析时请写清运行模式（真实模型 / mock）。
