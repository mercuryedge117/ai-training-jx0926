# Pipeline Demo 验收记录

日期：2026-09-12。实现对应 [Review spec](PIPELINE_DEMO_SPEC.md)，运行方式见 [README](README.md)。

最终验证：项目虚拟环境执行全仓库 `pytest`，**151 passed**（1 条来自既有 Starlette/httpx 的弃用提示）；离线短素材、长素材及长素材 Map-Reduce 三种 CLI 均退出 0；`git diff --check` 通过。

| 验收 | 结果 |
|---|---|
| 无凭据离线短素材 | 6 条消息保存，合成摘要命令返回原文摘录；FakeClient 不联网 |
| 离线长素材 / Map-Reduce | 长文本仍计 1 条；可以在消息内部切分，保留来源和 Map 产物；预算超限明确失败 |
| 真实 Slack 自发 Bot 素材 | 在 `#l2_demo` 显式发送一批新素材，`posted=6 / stored=6`，trace 来源均为 `slack_live` |
| 真人普通消息 | 用户已发送，成功保存，存储从 6 条增至 7 条 |
| 真人摘要命令 | `message` 分支跳过提及；`app_mention` 分支成功回复原 thread；没有把命令当作材料 |
| 防循环 | Bot 摘要回复到达后为 `ignored:unregistered_bot`，记录仍为 7 条 |
| 真实模型 | 通过已有 DeepSeek 配置总结长 fixture；结果仅写本地，没有额外发送到频道 |

真实运行证据位于本地 `course-demos/outputs/slack-demo/20260912T083136Z-aa7a0352/`。它是被 Git 忽略的运行产物，新 clone 使用随代码提交的 fixture，不依赖该路径。

真实模型初次输出被素材末尾“输出两版”的测试要求影响。加强系统层的单一受众和资料指令边界后重新测试：仅输出工程师版；人工核对保留了 1,260 请求 / 430 用户 / 512 订单的区别、回滚与恢复的区别、根因未确认、重复扣款待对账、FAQ 负责人未定。一次人工通过不代表完整质量评估或提示注入防护保证；篇幅仍可能超过 Prompt 的软限制，回帖另有长度上限。

自动化覆盖记录/命令分流、身份规则、重复投递、限流边界与用户隔离、登记竞争、队列满、存储/模型/回帖失败、未知发送结果、重载、离线禁止网络、慢模型与接收解耦、Reduce 超预算中间证据及“只有发送、没有投递”场景。

限时 Socket 验收进程已正常退出，不是持续托管服务；需要演示时按 README 重新启动。真实交互使用 mock 摘要，真实模型验证单独运行。消息编辑/删除、历史补齐、持久任务恢复仍不在 MVP 范围。
